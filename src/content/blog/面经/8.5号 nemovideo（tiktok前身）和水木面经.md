---
title: 8.5号 nemovideo（tiktok前身）和水木面经
slug: 8-5-hao-nemovideo-tiktok-qian-shen-he-shui-mu-mian-jing
description: 工作里面，你有没有什么印象最深，或者觉得最有挑战的项目，我们着重聊一下
publishDate: '2026-08-05'
category: 面经
tags:
  - React
  - Vue
  - JavaScript
  - Node.js
  - MySQL
  - Redis
  - CSS
  - 浏览器
cover: auto
draft: false
featured: false
toc: true
---
nemovideo问的比较久

工作里面，你有没有什么印象最深，或者觉得最有挑战的项目，我们着重聊一下

从输入url到页面渲染经过了哪些

什么是事件循环

面向生猪医药系统的开源项目，为什么要做这个项目，可以讲一下项目的难点、亮点

实习生问了很多非技术相关的问题（现在大几，是否可以线下实习等）

vue用到了虚拟dom，它为什么要用虚拟dom？

你刚刚说到vue中有数据更新，**vue中我们使用了双向绑定，这个双向绑定的实现原理你有了解过吗**

你知道reactive的实现原理吗

react、vue的依赖收集是怎么进行的你知道吗

vue的diff算法你可以讲一下你的了解程度

css做响应式布局的时候，常要注意的事情有哪些

em和rem有什么区别

如果要实现一个水平垂直居中，你要用什么方式去实现

如果要实现三栏式布局要怎么实现

BFC了解吗（块级格式化上下文）

如何触发这个块级格式化上下文，是用来解决什么问题的？

可以讲一下浏览器的事件循环机制

setTimeout是属于宏任务还是微任务

你有了解过node.js吗

node的事件循环和浏览器的事件循环有什么区别

js的原型链讲一下

new的时候做了什么

（创建实例、绑定原型、执行构造函数、返回对象）

new和原型链之间会发生什么联动吗

手写防抖

题目2：输入abcabcbb  输出3

最长无重复子串，要的是长度，不要字串的内容

```js
function debounce(fn,delay = 300){
    let timer = null
    return function(...args){
        if(timer) clearTimeout(timer)
        timer = setTimeout(() => {
            fn.apply(this,args)
            timer = null
        },delay)
    }
}

function lengthFn(s){
    const set = new Set()
    let left = 0
    let maxLength = 0
    for(let right = 0; right<s.length;rignth++){
        while(set.has(s[right])){
            set.delete(s[left])
            left++
        }
        set.add(s[right])
        maxLen = Math.max(maxLen,right - left + 1)
    }
    return maxLen
}
```

你了解canvas对象吗





本次面试问答完整汇总（前端实习·图形基础+算法方向）

一、基础认知类

1. 是否了解 Canvas ？
2. Canvas 元素拾取有哪几种主流实现思路？

二、几何判定法相关问题

1. 判断坐标点是否落在图形范围内，该算法时间复杂度是多少？
2. 除基础线性遍历几何判定外，有没有更高效的点图形包含判定方案？
3. 几何判定体系下，针对不同图形分别选用哪些包含检测算法？
4. AABB 包围盒做预筛选是否存在误判？原因是什么？
5. 多元素叠加场景下，包围盒粗筛会同时命中多个元素，后续如何精准筛选目标图形？

三、离屏画布像素索引 O(1)拾取方案（核心大题）

1. 依托多层画布、绘制阶段预埋数据，能否实现更高性能的拾取方案？整体思路是什么？
2. 纯单张Hit画布像素索引O(1)拾取方案存在什么缺陷？
3. 单张二维Hit画布同一坐标多个叠加元素会出现像素覆盖，仅能读取顶层元素；如何在保持O(1)拾取能力的前提下，获取该位置全部叠加元素？
4. 坐标存储元素ID列表的方案，随叠加层数提升会出现内存数据膨胀，如何缓解？
5. 利用点击交互是范围热区、无需单像素精准判定的特性，如何通过采样网格优化写入存储、降低内存膨胀？
6. 像素映射列表方案在绘制写入阶段，需要做哪些前置准备工作？

补充背景信息

1. 岗位：前端实习，AI引擎可视化方向
2. 考察侧重：底层技术原理、算法复杂度、时空性能优化，不侧重业务开发
3. 核心考点：Canvas渲染、图形拾取算法、时间/空间复杂度权衡、多层叠加场景优化、内存膨胀问题解决方案







## 水木（拉完了，没有保障，无转正）

面试问题完整汇总（全部面试官提问，无遗漏）

一、实习时间相关

1. 大四是不是基本都在校外实习？
2. 用友这份实习现在还在职吗？
3. 公司一般要毕业后才能签三方，是否可以接受？
4. 大四上半年是否能稳定全职实习？

二、Vue2 / Vue3 / React 技术核心问题

1. Vue2、Vue3、React三者主要区别是什么？从写法、底层实现、工程设计讲解
2. Vue3如何监听对象、数组的数据修改？
3. Vue2和Vue3还有哪些更多区别？
4. Vue3中监听DOM用什么方式？

三、项目经历、难点、开发角色相关

1. 过往项目中，遇到过哪些有挑战、难度高的工作？
2. 之前做的iOS开发是纯原生开发吗？
3. 是否做过iOS与JS混合桥接开发？
4. 在各个项目中担任什么角色？有没有0到1搭建项目，还是只负责单一模块？
5. 如果从零搭建完整前端工程，从目录划分角度，分为哪些纯技术通用模块（与业务无关）？
6. 你的开源项目里的数据是网上找的还是自己构造的？
7. 当初为什么选择做生猪智慧医药这类项目？
8. 项目里RAG问答是否实现SSE流式输出？
9. 项目中Markdown渲染看起来处理不完善是什么原因？
10. Markdown为什么需要转HTML？转换逻辑是前端处理还是后端？
11. 市面上有成熟Markdown渲染组件，为什么项目选择自己手动转换？

四、编程工具、AI开发工具

1. 是否经常使用AI编程编辑器，例如Codex？

五、后端基础储备

1. 对后端技术是否有了解（Node、Python）？
2. Node、MySQL、Redis掌握到什么程度？

六、公司业务、岗位发展沟通

1. 公司业务：生物医药+大模型、AI智能体垂直落地，多端产品（小程序/App/Web），核心为智能体前端开发，技术栈以React为主，听完后的匹配看法（面试官陈述业务，无提问）
2. 岗位后续工作范围不固定，无法保证只做纯前端，能否接受？



## 豆神教育已oc
